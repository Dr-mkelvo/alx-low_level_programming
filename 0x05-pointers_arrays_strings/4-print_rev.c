#include "main.h"
/**
 *
 *
 *
 *
 */
void print_rev(char *s)
{
	int i;
	int length = 0;
	while (s[length] != '\0')
		n++;
	for (i = length -1; i >= 0; i--)
	{	
		_putchar([i]);
	}
	_putchar('\0\);
}
