#include "main.h"

/**
 * mul - returns result of multiplication between 2 integers only
 *@a: first multiplier
 *@b: second multiplier
 *
 * Return: value for multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
